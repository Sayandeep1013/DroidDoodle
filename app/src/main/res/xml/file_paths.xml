<?xml version="1.0" encoding="utf-8"?>
<!--
  Only the export directory is shared. Deliberately not `.` -- models/ lives in
  filesDir and there is no reason for another app to be able to read a
  several-hundred-megabyte model through this provider.
-->
<paths>
    <cache-path name="export" path="export/" />
</paths>
