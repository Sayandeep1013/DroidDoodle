<?xml version="1.0" encoding="utf-8"?>
<!--
  Only the export directory is shared. Deliberately not the whole of filesDir:
  models/ lives there, and no other app has any reason to be able to read a
  several-hundred-megabyte model through this provider.
-->
<paths>
    <cache-path name="export" path="export/" />
</paths>
